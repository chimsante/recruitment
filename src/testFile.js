// testFile.js

// This is a simple test script to check if Node.js is working
console.log("Node.js is working!");

// You can also perform basic arithmetic to test further
const a = 5;
const b = 10;
console.log(`The sum of ${a} and ${b} is ${a + b}.`);
